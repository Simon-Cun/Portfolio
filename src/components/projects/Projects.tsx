import Title from "@/components/Title";
const Projects = () => {
  return (
    <section id="projects" className="left-0 w-full pl-10">
      <Title title="PROJECTS" />
    </section>
  );
};

export default Projects;
