import HorizontalTitle from "@/components/HorizontalTitle";

const Gallery = () => {
  return (
    <section
      id="gallery"
      className="flex h-screen w-full justify-center border-b-1 py-20"
    >
      <HorizontalTitle title="GALLERY" />
    </section>
  );
};

export default Gallery;
